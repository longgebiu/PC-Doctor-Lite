# 蓝屏代码速查表（高频 12 条）

> 当 PC-Doctor-Lite 检测到蓝屏记录时，用这张表快速定位原因。
> 格式：`代码 → 名称 → 人话解释 → 优先排查方向`

---

## 🔴 最常遇到的 5 个

### 0x0000007B — INACCESSIBLE_BOOT_DEVICE
**人话**：系统找不到启动盘了
**优先排查**：
1. BIOS 里 SATA 模式被改了（IDE/AHCI/RAID 切换）
2. 系统分区损坏 → 用 PE 盘修复引导
3. 硬盘快挂了 → 用 CrystalDiskInfo 看 SMART

### 0x00000050 — PAGE_FAULT_IN_NONPAGED_AREA
**人话**：系统想访问一块不该访问的内存
**优先排查**：
1. 内存条松动/坏了 → 拔下来擦金手指，换插槽
2. 最近装的驱动有 bug → 进安全模式卸载最近驱动
3. 用 Windows 内存诊断或 MemTest86 跑一遍

### 0x0000001A — MEMORY_MANAGEMENT
**人话**：内存管理出错了
**优先排查**：
1. 内存条故障（最常见）→ 换槽、单条测试
2. 硬盘有坏道导致虚拟内存出错
3. 系统文件损坏 → `sfc /scannow` + `DISM /Online /Cleanup-Image /RestoreHealth`

### 0x0000003B — SYSTEM_SERVICE_EXCEPTION
**人话**：系统服务出了异常
**优先排查**：
1. 显卡驱动不兼容 → DDU 彻底卸载后重装
2. 最近装了杀毒/防火墙软件 → 卸载试试
3. 系统更新不完整 → 补完更新

### 0x000000D1 — DRIVER_IRQL_NOT_LESS_OR_EQUAL
**人话**：某个驱动在错误的时机访问了内存
**优先排查**：
1. 网卡/显卡/声卡驱动 → 更新到最新版
2. 用 BlueScreenView 看"肇事驱动"列，精准定位
3. 卸载最近安装的硬件相关软件

---

## 🟡 次常见的 4 个

### 0x00000124 — WHEA_UNCORRECTABLE_ERROR
**人话**：硬件出了不可纠正的错误（通常是 CPU/内存/PCIe）
**优先排查**：
1. CPU 超频不稳定 → 恢复默认频率
2. 内存 XMP 不稳 → 关 XMP 测试
3. 电源供电不足 → 换电源测试

### 0x0000004E — PFN_LIST_CORRUPT
**人话**：内存页表损坏
**优先排查**：
1. 内存条故障 → 换槽测试
2. 驱动冲突 → 最近装了什么就卸什么

### 0x000000C2 — BAD_POOL_CALLER
**人话**：系统内存池被错误调用
**优先排查**：
1. 最近装的软件/驱动 → 卸载
2. 杀毒软件冲突 → 暂时关闭测试

### 0x000000F4 — CRITICAL_OBJECT_TERMINATION
**人话**：系统关键进程被强制终止
**优先排查**：
1. 硬盘故障 → 检查 SMART
2. 系统文件损坏 → sfc + DISM
3. 恶意软件 → 全盘杀毒

---

## 🟢 其他常见 3 个

### 0x0000007E — SYSTEM_THREAD_EXCEPTION_NOT_HANDLED
**人话**：系统线程抛了未处理的异常
**优先排查**：最近安装的驱动/更新 → 回滚

### 0x0000009F — DRIVER_POWER_STATE_FAILURE
**人话**：驱动在休眠/唤醒时没响应
**优先排查**：显卡/网卡驱动 → 更新；关闭快速启动测试

### 0x00000024 — NTFS_FILE_SYSTEM
**人话**：NTFS 文件系统损坏
**优先排查**：`chkdsk /f /r` 修复磁盘；硬盘可能有坏道

---

## 💡 通用排查流程（不管什么代码先看这 5 步）

1. **记下蓝屏代码和时间** → PC-Doctor-Lite 会自动记录
2. **用 BlueScreenView 看"肇事驱动"** → 精准定位到 .sys 文件
3. **最近改了什么？** → 装了新驱动/软件/硬件就先回滚
4. **跑内存诊断** → Windows 自带或 MemTest86
5. **查硬盘健康** → CrystalDiskInfo 看 SMART 有没有红黄预警

---

*本速查表随 PC-Doctor-Lite v1.0 附赠，仅供个人学习参考。*
*蓝屏代码含义以微软官方文档为准。*
